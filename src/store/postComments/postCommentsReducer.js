const initialState = {
  post: null,
  comments: [],
  loading: false,
  error: '',
};

export const postCommentsReudcer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
